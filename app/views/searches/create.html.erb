<h1>Create Search Page</h1>

<%= link_to 'Back', new_search_path %>

 <table class="table table-hover">
  	<tr>
  		<th>Tweets</th>
  	</tr>

    <% @tweetsearch.each do |tweet| %>
    <tr>
    	<td><%= link_to tweet.text, "https://www.twitter.com/#{tweet.user.screen_name}" %></td>
    </tr>
   <% end %>   
  </table>